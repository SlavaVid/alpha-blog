<h1 class="text-center mt-4 text"><%= @user.username %>'s profile</h1>

<%= gravatar_for @user, size: 200 %> 

<div class = "text-center mt-4 text">
<%= link_to "Edit your profile",edit_user_path(@user),class: "btn btn-warning" %>
</div>

<h3 class="text-center mt-4 text">Articles</h3>

<%= render 'articles/article'%>